import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { CurrentrentedPage } from './currentrented';

@NgModule({
  declarations: [
    CurrentrentedPage,
  ],
  imports: [
    IonicPageModule.forChild(CurrentrentedPage),
  ],
})
export class CurrentrentedPageModule {}
